---
inventory:
    plugin: AnsibleInventory
    options:
        hostsfile: "inventory/ansible_inv.yaml"
        # host_file: "inventory/hosts.yaml"
        # group_file: "inventory/groups.yaml"
        # defaults_file: "inventory/defaults.yaml"
runner:
    plugin: threaded
    options:
        num_workers: 100